import React from 'react';
import FormField from '../Input/FormField';

const SendEmail = ({form}) => {
  return (
    <div>
      hello world in send email
    </div>
  );
};

export default SendEmail;
